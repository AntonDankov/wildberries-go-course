## Flags

- `-host` - Server hostname or IP address (required)
- `-port` - Server port number (required)
- `-timeout` - Connection timeout in seconds (default: 10)

## How to Run

Example:

```bash
go run . -host tcpbin.com -port 4242
```
